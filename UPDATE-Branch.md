## knife-branch

### 基础主分支
```
合并 20241011 main 更新 v2.3.15
```

### 额外更新
```
v2.3.14 
    尝试修复请求头去除的可能BUG

v2.3.11
    增加无痕删除请求头功能
    限制清空响应体内容仅用于基于请求方法修改响应头功能
    修改一些表单名称

v2.3.12
    添加删除响应头功能
    
v2.3.10
    增加条件判断，支持关闭和开启 消息面板中的 chinese和info 面板。

v2.3.9 - v2.3.7
    增加旧功能 添加默认排除列表的功能
    增加响应体置空的选项，不建议使用
    修复自动加载配置文件在合并新代码时被删除的问题

v2.3.6
    使用旧版本 knife-plus 方案 修改options 解决OPTIONS无法正常过滤问题

v2.3.4
    取消默认启用选项

v2.3.3
    1、配置插件优化UI线程安全加载
    2、移植knife-plus部分功能
        在Auto_Load_Project_Config为True的情况下:
        支持每次插件启动时进行加载项目配置文件
        支持右键菜单手动保存项目配置文件
        支持右键菜单手动加载项目配置文件
        支持右键增加scope include域名 (正则)
        支持右键增加scope exclude域名 (正则)
        支持右键清空scope 所有范围
        支持通过配置Add_Hosts_Exclude_Scope添加默认需要排除scope exclude域名 (域名正则格式)
    3、用中文描述了部分内置属性
```